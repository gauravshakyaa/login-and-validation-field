# login-and-validation-field

[project]
name = "assignment-task-1-and-2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pytest-html>=4.1.1",
    "pytest>=8.3.5",
    "selenium>=4.29.0",
]
